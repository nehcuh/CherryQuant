# Module 0: 前置知识与环境搭建

## 课程信息

- **模块编号**: Module 0
- **难度**: ⭐ 入门
- **预计时间**: 4-6 小时
- **前置要求**: 无

## 学习目标

完成本模块后，你将能够：

1. ✅ 理解 CherryQuant 项目的教学定位和学习路径
2. ✅ 掌握 Python 3.12+ 的基础特性
3. ✅ 成功搭建开发环境（Python、uv、Docker）
4. ✅ 配置项目依赖和环境变量
5. ✅ 运行第一个 CherryQuant 示例程序
6. ✅ 理解项目目录结构和核心组件

## 课程大纲

### 第一部分：项目介绍与学习路径 (30 分钟)

#### 1.1 CherryQuant 是什么？

CherryQuant 是一个**教学项目**，旨在帮助大学生学习：

- **AI 编程技能**: 大语言模型（LLM）集成、提示工程
- **Python 最佳实践**: 类型提示、异步编程、现代工具链
- **软件架构设计**: 六边形架构、依赖注入、测试驱动开发
- **量化交易概念**: 期货市场、风险管理、策略开发

**⚠️ 重要声明**: 本项目**不是**生产级交易系统，**禁止**用于真实资金交易。

#### 1.2 为什么要学习这个项目？

**区别于传统课程的特点**:

| 传统课程 | CherryQuant 教学项目 |
|---------|---------------------|
| 玩具示例（Hello World） | 真实复杂度的项目 |
| 单一技术点 | 综合技术栈 |
| 理论为主 | 理论 + 实践 |
| 考试驱动 | 兴趣驱动 |
| 毕业即过时 | 与业界同步 |

**项目的独特价值**:

1. **AI 驱动**: 不同于传统量化（纯数学模型），使用 GPT-4 进行决策
2. **现代工具链**: uv 包管理、structlog 日志、MongoDB 时序数据库
3. **生产级架构**: 六边形架构、依赖注入、完善的测试
4. **真实业务**: 对接中国期货市场，处理真实数据

#### 1.3 10 周学习路径

```
Week 1: 环境搭建 + 基础概念        [本模块]
Week 2: 系统架构设计              [Module 1]
Week 3-4: 数据管道                [Module 2]
Week 5-6: AI 决策引擎             [Module 3]
Week 7-8: 交易执行与风险管理       [Module 4]
Week 9: 依赖注入与测试            [Module 5, 6]
Week 10: 毕业项目                 [Module 9]
```

**学习建议**:
- 每周投入 10-12 小时
- 完成课程模块 + 实验 + 示例代码
- 记录学习笔记和遇到的问题
- 积极在课程论坛讨论

---

### 第二部分：前置知识检查 (1 小时)

#### 2.1 必备知识

**Python 基础** (必须)
- [ ] 变量、数据类型、控制流
- [ ] 函数定义和调用
- [ ] 类和对象（基础 OOP）
- [ ] 列表、字典等数据结构
- [ ] 异常处理 (try/except)

**推荐资源**:
- [Python 官方教程](https://docs.python.org/zh-cn/3/tutorial/)
- [廖雪峰 Python 教程](https://www.liaoxuefeng.com/wiki/1016959663602400)

**Linux/命令行** (必须)
- [ ] 基本命令: `cd`, `ls`, `mkdir`, `cat`
- [ ] 环境变量设置
- [ ] 包管理工具使用

**Git 版本控制** (推荐)
- [ ] `git clone`, `git pull`, `git commit`
- [ ] 分支概念

#### 2.2 进阶知识（本课程将教授）

以下内容如果不熟悉没关系，课程中会详细讲解：

- ⏭️ Python 类型提示 (Type Hints)
- ⏭️ 异步编程 (async/await)
- ⏭️ 装饰器 (Decorators)
- ⏭️ 上下文管理器 (Context Managers)
- ⏭️ 软件架构模式
- ⏭️ 量化交易概念

#### 2.3 自我评估

**Python 基础测试**（5 分钟）:

```python
# 问题 1: 以下代码输出什么？
def greet(name="World"):
    return f"Hello, {name}!"

print(greet())
print(greet("Alice"))

# 问题 2: 修复以下代码的错误
class Calculator:
    def add(a, b):  # 缺少 self 参数
        return a + b

calc = Calculator()
result = calc.add(3, 5)

# 问题 3: 实现一个函数，返回列表中的偶数
def get_evens(numbers):
    # 你的代码
    pass

assert get_evens([1, 2, 3, 4, 5]) == [2, 4]
```

**答案和解析** 见附录 A。

如果以上问题有困难，建议先补充 Python 基础。

---

### 第三部分：开发环境搭建 (2-3 小时)

#### 3.1 系统要求

**操作系统**:
- ✅ macOS 12+
- ✅ Ubuntu 20.04+ / Debian 11+
- ✅ Windows 11 (WSL2)

**硬件要求**:
- CPU: 2 核心以上
- 内存: 8GB 以上 (推荐 16GB)
- 磁盘: 20GB 可用空间

#### 3.2 安装 Python 3.12+

**macOS** (推荐使用 Homebrew):
```bash
# 安装 Homebrew (如果未安装)
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# 安装 Python 3.12
brew install python@3.12

# 验证安装
python3.12 --version
# 输出: Python 3.12.x
```

**Ubuntu/Debian**:
```bash
# 添加 deadsnakes PPA
sudo add-apt-repository ppa:deadsnakes/ppa
sudo apt update

# 安装 Python 3.12
sudo apt install python3.12 python3.12-venv python3.12-dev

# 验证安装
python3.12 --version
```

**Windows (WSL2)**:
```bash
# 在 WSL2 Ubuntu 环境中执行上述 Ubuntu 命令
```

#### 3.3 安装 uv 包管理器

[uv](https://github.com/astral-sh/uv) 是 Rust 编写的超快 Python 包管理器。

```bash
# macOS/Linux 一键安装
curl -LsSf https://astral.sh/uv/install.sh | sh

# 或使用 pip 安装
pip install uv

# 验证安装
uv --version
# 输出: uv 0.x.x
```

**为什么使用 uv？**

| 特性 | pip | uv |
|------|-----|----|
| 安装速度 | 慢 | 极快 (10-100x) |
| 依赖解析 | 基础 | 强大 |
| 锁文件 | 无 | uv.lock |
| 虚拟环境管理 | 手动 | 自动 |

#### 3.4 安装 Docker 和 Docker Compose

**为什么需要 Docker？**
- 运行 MongoDB (数据存储)
- 运行 Redis (缓存)
- 隔离环境，避免污染系统

**macOS**:
```bash
# 下载并安装 Docker Desktop
# https://www.docker.com/products/docker-desktop/

# 验证安装
docker --version
docker-compose --version
```

**Ubuntu/Debian**:
```bash
# 安装 Docker
curl -fsSL https://get.docker.com | sh

# 添加当前用户到 docker 组
sudo usermod -aG docker $USER
newgrp docker

# 安装 Docker Compose
sudo apt install docker-compose-plugin

# 验证安装
docker --version
docker compose version
```

#### 3.5 安装代码编辑器（推荐）

**VS Code** (推荐):
- 下载: https://code.visualstudio.com/
- 推荐插件:
  - Python (ms-python.python)
  - Pylance (ms-python.vscode-pylance)
  - Docker (ms-azuretools.vscode-docker)

**PyCharm** (可选):
- 功能更强大，但较重

**配置 VS Code**:
```json
// .vscode/settings.json
{
  "python.defaultInterpreterPath": "${workspaceFolder}/.venv/bin/python",
  "python.linting.enabled": true,
  "python.linting.mypyEnabled": true,
  "python.formatting.provider": "black",
  "editor.formatOnSave": true
}
```

---

### 第四部分：CherryQuant 项目环境配置 (1-2 小时)

#### 4.1 克隆项目

```bash
# 使用 HTTPS (推荐)
git clone https://github.com/your-username/CherryQuant.git
cd CherryQuant

# 或使用 SSH (如果配置了 SSH Key)
git clone git@github.com:your-username/CherryQuant.git
cd CherryQuant
```

#### 4.2 一键环境配置（学生推荐）

项目提供了自动化配置脚本：

```bash
bash scripts/teaching/student_setup.sh
```

该脚本会自动：
- ✅ 检查 Python 版本
- ✅ 安装 uv 包管理器
- ✅ 创建虚拟环境
- ✅ 安装所有依赖
- ✅ 创建 `.env` 配置文件模板
- ✅ 启动数据库服务 (Docker)

#### 4.3 手动配置（进阶）

如果自动脚本失败，或想了解细节，可以手动配置：

**步骤 1: 创建虚拟环境**
```bash
# uv 会自动创建虚拟环境
uv venv

# 激活虚拟环境 (可选，uv run 会自动使用)
source .venv/bin/activate  # macOS/Linux
# .venv\Scripts\activate   # Windows
```

**步骤 2: 安装依赖**
```bash
# 安装所有依赖 (包括开发依赖)
uv sync

# 仅安装生产依赖
uv sync --no-dev
```

**步骤 3: 配置环境变量**
```bash
# 复制模板
cp .env.example .env

# 编辑配置
vim .env  # 或使用你喜欢的编辑器
```

**步骤 4: 启动数据库服务**
```bash
# 启动 MongoDB 和 Redis
docker-compose up -d mongodb redis

# 验证服务状态
docker-compose ps

# 查看日志
docker-compose logs mongodb
```

#### 4.4 环境变量配置详解

**最小配置** (`.env` 文件):

```bash
# === 基础配置 ===
LOG_LEVEL=INFO
DEBUG=true

# === 数据库配置 ===
MONGO_HOST=localhost
MONGO_PORT=27017
MONGO_DB_NAME=cherryquant_dev

REDIS_HOST=localhost
REDIS_PORT=6379

# === 数据源 (暂时留空，后续课程配置) ===
# TUSHARE_TOKEN=
# OPENAI_API_KEY=
```

**配置说明**:

| 变量名 | 说明 | 默认值 | 是否必需 |
|--------|------|--------|---------|
| `LOG_LEVEL` | 日志级别 (DEBUG/INFO/WARNING) | INFO | 否 |
| `DEBUG` | 调试模式 | false | 否 |
| `MONGO_HOST` | MongoDB 主机 | localhost | 是 |
| `MONGO_PORT` | MongoDB 端口 | 27017 | 是 |
| `MONGO_DB_NAME` | 数据库名称 | cherryquant | 是 |
| `REDIS_HOST` | Redis 主机 | localhost | 是 |
| `REDIS_PORT` | Redis 端口 | 6379 | 是 |
| `TUSHARE_TOKEN` | Tushare API Token | - | Module 2 需要 |
| `OPENAI_API_KEY` | OpenAI API Key | - | Module 3 需要 |

**安全提示**:
- ⚠️ **永远不要**提交 `.env` 文件到 Git
- ⚠️ `.gitignore` 已包含 `.env`，确保不要误提交
- ⚠️ 不要在 `.env` 中包含真实的交易账户信息

---

### 第五部分：验证环境与首次运行 (30 分钟)

#### 5.1 运行第一个示例

```bash
# 运行 Hello CherryQuant 示例
uv run python examples/01_basics/hello_cherryquant.py
```

**预期输出**:

```
============================================================
🍒 欢迎使用 CherryQuant AI 量化交易教学项目！
============================================================

📋 项目基本信息:
  • 项目根目录: /path/to/CherryQuant
  • 日志级别: INFO
  • 是否启用调试模式: True

🗄️  数据库配置:
  • MongoDB 数据库: cherryquant_dev
  • Redis 主机: localhost

📊 支持的期货品种 (共 X 个):
  • 黑色系: rb, hc, i, j, jm
  • 有色金属: cu, al, zn, pb, ni, sn
  ...

============================================================
✅ 示例运行成功！
============================================================

💡 下一步:
  1. 阅读 docs/course/01_System_Architecture.md 了解系统架构
  2. 运行 examples/02_data/ 下的数据获取示例
  3. 完成 Lab 01 实验任务
```

#### 5.2 环境验证清单

运行验证脚本:

```bash
# 运行环境检查脚本
uv run python scripts/teaching/verify_environment.py
```

该脚本会检查:
- [x] Python 版本 >= 3.12
- [x] uv 包管理器已安装
- [x] 所有依赖已安装
- [x] `.env` 文件存在
- [x] MongoDB 可连接
- [x] Redis 可连接
- [x] 项目目录结构完整

**如果验证失败**，查看错误信息并修复问题。

#### 5.3 理解项目目录结构

```
CherryQuant/
├── config/                 # 配置管理
│   ├── settings/           # 设置模块
│   ├── symbols.py          # 期货品种配置
│   └── alert_config.py     # 告警配置
│
├── src/cherryquant/        # 核心代码
│   ├── __init__.py
│   ├── bootstrap/          # 依赖注入启动
│   ├── adapters/           # 适配器 (数据、存储)
│   ├── ai/                 # AI 决策引擎
│   └── services/           # 业务服务
│
├── examples/               # 示例代码
│   ├── 01_basics/          # 基础示例
│   ├── 02_data/            # 数据示例
│   ├── 03_ai/              # AI 示例
│   ├── 04_trading/         # 交易示例
│   └── complete_system/    # 完整系统
│
├── docs/                   # 文档
│   ├── course/             # 课程模块
│   ├── labs/               # 实验指导
│   ├── adr/                # 架构决策记录
│   └── reference/          # 参考文档
│
├── tests/                  # 测试代码
│   ├── unit/               # 单元测试
│   └── integration/        # 集成测试
│
├── scripts/                # 工具脚本
│   ├── teaching/           # 教学辅助脚本
│   └── init_*.py           # 初始化脚本
│
├── docker/                 # Docker 配置
│   └── docker-compose.*.yml
│
├── .env                    # 环境变量 (不提交到 Git)
├── .env.example            # 环境变量模板
├── pyproject.toml          # 项目配置
├── uv.lock                 # 依赖锁定文件
├── README.md               # 项目说明
└── LEARNING_PATH.md        # 学习路径指南
```

**核心目录说明**:

- **`config/`**: 所有配置文件，包括期货品种、风险参数等
- **`src/cherryquant/`**: 核心业务逻辑，遵循六边形架构
  - `bootstrap/`: 依赖注入的 Composition Root
  - `adapters/`: 外部系统适配器（数据源、数据库）
  - `ai/`: AI 决策引擎和 LLM 客户端
  - `services/`: 业务服务层
- **`examples/`**: 按学习路径组织的示例代码
- **`docs/`**: 课程文档、实验指导、架构决策
- **`tests/`**: 单元测试和集成测试

---

### 第六部分：核心概念预览 (1 小时)

#### 6.1 什么是六边形架构？

**传统分层架构** vs **六边形架构**:

```
传统分层架构:
UI 层 → 业务逻辑层 → 数据访问层 → 数据库
(依赖方向单一，难以测试和替换)

六边形架构 (Hexagonal Architecture):
        ┌─────────────────┐
        │   核心业务逻辑    │
        │  (领域模型)      │
        └─────────────────┘
             ↑       ↑
             │ Ports │ (接口)
             ↓       ↓
    ┌─────────┐  ┌─────────┐
    │ 输入适配器 │  │ 输出适配器 │
    │  (API)   │  │ (数据库) │
    └─────────┘  └─────────┘
```

**核心思想**:
- **业务逻辑不依赖外部**: 核心逻辑不知道数据来自哪里、存储到哪里
- **通过接口（Ports）通信**: 业务逻辑定义接口，适配器实现接口
- **易于测试**: 可以轻松替换适配器为测试桩（Mock）

**CherryQuant 中的应用**:

```python
# 核心业务逻辑定义接口
class DataAdapter(Protocol):
    async def fetch_kline(self, symbol: str) -> List[KlineData]:
        ...

# 适配器实现接口
class TushareAdapter(DataAdapter):
    async def fetch_kline(self, symbol: str) -> List[KlineData]:
        # 调用 Tushare API
        ...

class VNPyAdapter(DataAdapter):
    async def fetch_kline(self, symbol: str) -> List[KlineData]:
        # 调用 VNPy 接口
        ...

# 业务逻辑只依赖接口
class HistoryDataManager:
    def __init__(self, adapter: DataAdapter):
        self.adapter = adapter  # 依赖注入

    async def get_history(self, symbol: str):
        return await self.adapter.fetch_kline(symbol)
```

**Module 1 和 Module 5 会详细讲解**。

#### 6.2 什么是依赖注入？

**问题**: 硬编码依赖导致难以测试和替换

```python
# ❌ 不好的设计：硬编码依赖
class TradingSystem:
    def __init__(self):
        self.database = MongoDBManager()  # 硬编码
        self.ai_engine = AIEngine()        # 硬编码

# 测试困难，无法替换为 Mock
```

**解决**: 依赖注入（Dependency Injection）

```python
# ✅ 好的设计：依赖注入
class TradingSystem:
    def __init__(
        self,
        database: DatabaseManager,  # 注入依赖
        ai_engine: AIEngine          # 注入依赖
    ):
        self.database = database
        self.ai_engine = ai_engine

# 使用时注入真实依赖
system = TradingSystem(
    database=MongoDBManager(),
    ai_engine=AIEngine()
)

# 测试时注入 Mock
system_test = TradingSystem(
    database=MockDatabase(),
    ai_engine=MockAI()
)
```

**Composition Root 模式**:

在应用启动时，统一创建和连接所有依赖（见 `bootstrap/app_context.py`）。

#### 6.3 什么是提示工程？

**传统机器学习** vs **大语言模型**:

```
传统 ML:
数据准备 → 特征工程 → 模型训练 → 模型部署
(需要大量数据和专业知识)

LLM + 提示工程:
设计提示词 → 调用 API → 获取结果
(零样本或少样本学习)
```

**CherryQuant 的 AI 策略**（详见 ADR-0003）:

- ✅ 使用提示工程，而非模型微调
- ✅ 系统提示词定义 AI 角色和分析框架
- ✅ 用户提示词提供市场数据和技术指标
- ✅ AI 返回结构化的交易建议（JSON 格式）

**示例**:

```python
SYSTEM_PROMPT = """
你是一个专业的期货交易分析师。

分析框架：
1. 趋势分析
2. 动量分析
3. 风险评估
4. 决策建议

输出格式：
{
  "action": "BUY/SELL/HOLD",
  "confidence": 0.0-1.0,
  "reasoning": "分析理由"
}
"""

USER_PROMPT = f"""
品种: 螺纹钢 (rb2501)
当前价格: 3500
涨跌幅: +1.2%
MA5: 3480, MA20: 3450
RSI: 65

请分析并给出交易建议。
"""

# GPT-4 返回
{
  "action": "BUY",
  "confidence": 0.75,
  "reasoning": "价格突破 MA20，RSI 未超买，建议做多"
}
```

**Module 3 会详细教授提示工程技巧**。

---

## 实践练习

### Lab 01: 环境搭建与首次运行 (2 小时)

**目标**: 完成环境搭建，成功运行示例程序

**任务清单**:

- [ ] **任务 1**: 安装 Python 3.12、uv、Docker
- [ ] **任务 2**: 克隆项目并配置环境
- [ ] **任务 3**: 启动数据库服务（MongoDB、Redis）
- [ ] **任务 4**: 运行 `hello_cherryquant.py` 并截图输出
- [ ] **任务 5**: 运行环境验证脚本 `verify_environment.py`
- [ ] **任务 6**: 理解项目目录结构，绘制思维导图
- [ ] **任务 7**: 阅读 `README.md` 和 `LEARNING_PATH.md`

**提交内容**:
1. 环境验证脚本的完整输出
2. `hello_cherryquant.py` 的运行截图
3. 项目目录结构的思维导图（手绘或工具绘制）
4. 学习笔记（至少 500 字）

**评分标准** (10 分):
- 环境正确配置 (4 分)
- 示例成功运行 (2 分)
- 目录结构理解 (2 分)
- 学习笔记质量 (2 分)

**常见问题**:

1. **Docker 启动失败**: 检查 Docker Desktop 是否运行
2. **uv 命令找不到**: 确保 PATH 配置正确
3. **Python 版本不对**: 使用 `python3.12` 而非 `python`

---

## 自我评估

完成本模块后，请自我检查：

- [ ] 我理解 CherryQuant 的教学定位和学习路径
- [ ] 我的开发环境已正确配置
- [ ] 我能成功运行第一个示例程序
- [ ] 我理解项目的目录结构
- [ ] 我对六边形架构、依赖注入、提示工程有初步了解
- [ ] 我知道在哪里寻求帮助（课程论坛、文档、老师）

## 下一步

完成本模块后，继续学习：

- **Module 1**: [系统架构设计](./01_System_Architecture.md)
- **Lab 02**: 追踪数据流
- **示例代码**: `examples/02_data/`

---

## 附录

### 附录 A: Python 基础测试答案

**问题 1 答案**:
```
Hello, World!
Hello, Alice!
```

**问题 2 答案**:
```python
class Calculator:
    def add(self, a, b):  # 添加 self 参数
        return a + b
```

**问题 3 答案**:
```python
def get_evens(numbers):
    return [n for n in numbers if n % 2 == 0]
```

### 附录 B: 推荐学习资源

**Python 进阶**:
- [Fluent Python (流畅的 Python)](https://book.douban.com/subject/27028517/)
- [Python Type Hints 官方文档](https://docs.python.org/3/library/typing.html)

**软件架构**:
- [Clean Architecture (整洁架构)](https://book.douban.com/subject/30333919/)
- [Hexagonal Architecture (六边形架构)](https://alistair.cockburn.us/hexagonal-architecture/)

**量化交易**:
- [Python for Finance (金融 Python)](https://www.oreilly.com/library/view/python-for-finance/9781492024323/)

**AI 与提示工程**:
- [OpenAI Prompt Engineering Guide](https://platform.openai.com/docs/guides/prompt-engineering)
- [LangChain 官方文档](https://python.langchain.com/)

### 附录 C: 常用命令速查

```bash
# uv 包管理
uv sync                    # 安装依赖
uv add <package>           # 添加包
uv run <command>           # 运行命令

# Docker
docker-compose up -d       # 启动服务
docker-compose down        # 停止服务
docker-compose ps          # 查看状态
docker-compose logs -f     # 查看日志

# Git
git clone <url>            # 克隆项目
git pull                   # 拉取最新代码
git status                 # 查看状态

# Python
python3.12 --version       # 检查版本
python -m venv .venv       # 创建虚拟环境
source .venv/bin/activate  # 激活虚拟环境
```

---

**课程支持**:
- 📧 教师邮箱: [邮箱地址]
- 💬 课程论坛: [论坛链接]
- 📚 文档仓库: [GitHub 链接]

**祝学习顺利！🎓**
