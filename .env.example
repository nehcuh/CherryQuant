# CherryQuant 环境变量配置示例
# 复制此文件为 .env 并填入您的实际配置

# ===== 运行环境配置 =====
ENVIRONMENT=development          # 运行环境: development, production
DEBUG=false                      # 调试模式
TIMEZONE=Asia/Shanghai           # 时区

# ===== 数据模式配置 =====
DATA_MODE=dev                    # 数据模式: live(CTP实时) | dev(Tushare准实时)
DATA_SOURCE=tushare              # 数据源类型: tushare, vnpy, simnow

# ===== MongoDB 数据库配置 =====
MONGODB_URI=mongodb://localhost:27017
MONGODB_DATABASE=cherryquant
MONGODB_MIN_POOL_SIZE=5
MONGODB_MAX_POOL_SIZE=50
# MONGODB_USERNAME=your_username    # 生产环境建议配置
# MONGODB_PASSWORD=your_password    # 生产环境建议配置

# ===== Redis 缓存配置 =====
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
# REDIS_PASSWORD=your_redis_password  # 如需密码验证
DATA_CACHE_TTL=300               # 缓存TTL（秒）

# ===== AI 配置 =====
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4
AI_TEMPERATURE=0.1
MAX_RETRIES=3
API_TIMEOUT=30

# ===== 交易配置 =====
DEFAULT_SYMBOL=rb2501
EXCHANGE=SHFE
DECISION_INTERVAL=300            # 决策间隔(秒)
MAX_POSITION_SIZE=10             # 最大持仓手数
DEFAULT_LEVERAGE=5.0             # 默认杠杆
RISK_PER_TRADE=0.02              # 每笔交易风险比例

# ===== Tushare 配置 =====
TUSHARE_TOKEN=your_tushare_pro_token_here

# ===== CTP 配置 (live 模式需要) =====
CTP_USERID=your_ctp_userid
CTP_PASSWORD=your_ctp_password
CTP_BROKER_ID=9999               # SimNow 默认为 9999
CTP_MD_ADDRESS=tcp://180.168.146.187:10131  # 行情服务器
CTP_TD_ADDRESS=tcp://180.168.146.187:10130  # 交易服务器

# ===== 组合风险管理配置 =====
PORTFOLIO_MAX_CAPITAL_USAGE=0.8       # 最大总资金使用率 (0-1)
PORTFOLIO_MAX_CORRELATION=0.7         # 最大相关性阈值 (0-1)
PORTFOLIO_MAX_SECTOR_CONCENTRATION=0.4  # 最大单一板块集中度 (0-1)
PORTFOLIO_STOP_LOSS=0.1               # 组合止损比例 (0-0.5)
PORTFOLIO_DAILY_LOSS_LIMIT=0.05       # 每日亏损限制 (0-0.5)
PORTFOLIO_MAX_LEVERAGE=3.0            # 总杠杆限制 (1-10)

# ===== 日志配置 =====
LOG_LEVEL=INFO                   # 日志级别: DEBUG, INFO, WARNING, ERROR
LOG_DIR=./logs                   # 日志目录
LOG_MAX_BYTES=10485760           # 单个日志文件最大字节数 (10MB)
LOG_BACKUP_COUNT=5               # 保留日志文件数量
