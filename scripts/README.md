# 📜 Scripts 脚本目录

CherryQuant 项目的所有脚本文件，按功能分类组织。

---

## 📂 目录结构

```
scripts/
├── README.md              # 本文件 - 脚本索引
│
├── runners/              🚀 系统运行脚本（不同模式）
│   ├── run_cherryquant_complete.py
│   ├── run_cherryquant_ai_selection.py
│   └── run_cherryquant_multi_agent.py
│
├── data/                 💾 数据处理脚本
│   └── init_historical_data.py
│
├── utils/                🔧 工具脚本
│   ├── fix_env_config.py
│   ├── migrate_postgres_to_mongodb.py
│   └── refactor_imports.py
│
├── tests/                🧪 手动测试脚本
│   └── test_risk_config.py
│
├── teaching/             🎓 教学辅助脚本
│   ├── student_setup.sh
│   └── verify_environment.py
│
└── sql/                  🗄️ SQL 脚本
    ├── create_agent_tables.sql
    └── create_portfolio_tables.sql
```

---

## 🚀 运行脚本（Runners）

### 主运行脚本（根目录）

#### `run_cherryquant.py` ⭐ 推荐
**位置**: 项目根目录
**用途**: 主运行脚本，支持多种交易模式
**使用方式**:
```bash
# 模拟交易模式（推荐学习使用）
uv run python run_cherryquant.py simulation

# 回测模式
uv run python run_cherryquant.py backtest

# 实盘模式（需要真实账户，不推荐学生使用）
uv run python run_cherryquant.py live
```

**功能**:
- ✅ 多模式支持（模拟/回测/实盘）
- ✅ 完整的数据管道集成
- ✅ AI 决策引擎
- ✅ 风险管理系统
- ✅ 适合日常使用和教学演示

---

#### `start.sh` 🎯 新手友好
**位置**: 项目根目录
**用途**: 一键启动脚本，交互式选择运行模式
**使用方式**:
```bash
bash start.sh
```

**功能**:
- ✅ 自动检查环境（uv, Python 3.12）
- ✅ 自动安装依赖
- ✅ 交互式选择模式
- ✅ 配置验证
- ✅ 最适合初学者和快速启动

**运行流程**:
```
1. 检查 uv 是否安装
2. 检查 Python 3.12
3. 安装项目依赖 (uv sync)
4. 检查 .env 配置文件
5. 交互式选择运行模式
6. 启动系统
```

---

### 高级运行脚本（Runners）

以下脚本位于 `scripts/runners/`，提供特定功能的运行模式：

#### `run_cherryquant_complete.py` 🎛️ 完整版
**用途**: 完整的多代理AI交易系统，集成所有高级功能
**使用方式**:
```bash
uv run python scripts/runners/run_cherryquant_complete.py
```

**功能**:
- ✅ 多代理管理（AgentManager）
- ✅ 组合风险管理（PortfolioRiskManager）
- ✅ 警报系统（AlertManager）
- ✅ Web 监控界面
- ✅ 完整的日志和监控

**适用场景**:
- 高级用户
- 需要完整功能的场景
- 多策略组合管理
- 实时监控需求

---

#### `run_cherryquant_ai_selection.py` 🤖 AI选择版
**用途**: 基于AI选股的交易系统
**使用方式**:
```bash
uv run python scripts/runners/run_cherryquant_ai_selection.py
```

**功能**:
- ✅ AI 选股引擎
- ✅ 动态品种筛选
- ✅ 智能仓位管理

**适用场景**:
- 研究AI选股策略
- 动态品种池管理
- 教学：AI在选股中的应用

---

#### `run_cherryquant_multi_agent.py` 🎭 多Agent版
**用途**: 多Agent协作的交易系统
**使用方式**:
```bash
uv run python scripts/runners/run_cherryquant_multi_agent.py
```

**功能**:
- ✅ 多个交易Agent并行运行
- ✅ Agent间协调和冲突解决
- ✅ 分布式决策

**适用场景**:
- 研究多Agent系统
- 不同策略组合
- 教学：分布式AI系统

---

## 💾 数据处理脚本（Data）

### `init_historical_data.py` 📊
**位置**: `scripts/data/`
**用途**: 初始化历史数据，从数据源下载并导入数据库
**使用方式**:
```bash
uv run python scripts/data/init_historical_data.py
```

**功能**:
- 从 Tushare/QuantBox 获取历史K线数据
- 批量导入 MongoDB
- 数据验证和去重
- 进度显示

**参数**:
```bash
# 指定品种
uv run python scripts/data/init_historical_data.py --symbols rb2501,hc2501

# 指定日期范围
uv run python scripts/data/init_historical_data.py --start 20240101 --end 20241231

# 全量初始化
uv run python scripts/data/init_historical_data.py --all
```

**适用场景**:
- 首次使用项目，初始化数据
- 定期更新历史数据
- 添加新品种

---

## 🔧 工具脚本（Utils）

### `fix_env_config.py` 🔨
**位置**: `scripts/utils/`
**用途**: 修复 .env 文件中的重复配置
**使用方式**:
```bash
uv run python scripts/utils/fix_env_config.py
```

**功能**:
- 检测 .env 文件中的重复配置项
- 交互式选择处理方式
- 自动清理重复项

**使用场景**:
- .env 文件配置混乱时
- 合并多个配置源后
- 配置项冲突时

---

### `migrate_postgres_to_mongodb.py` 🔄
**位置**: `scripts/utils/`
**用途**: 数据库迁移工具（PostgreSQL → MongoDB）
**使用方式**:
```bash
uv run python scripts/utils/migrate_postgres_to_mongodb.py
```

**功能**:
- 从 PostgreSQL 导出数据
- 转换数据格式
- 导入到 MongoDB

**使用场景**:
- 项目早期版本使用 PostgreSQL 的用户
- 数据库迁移
- 历史数据保留

**注意**: 当前项目已完全迁移到 MongoDB，此脚本仅供参考

---

### `refactor_imports.py` 🔍
**位置**: `scripts/utils/`
**用途**: 重构代码中的导入语句
**使用方式**:
```bash
uv run python scripts/utils/refactor_imports.py
```

**功能**:
- 批量修改导入路径
- 统一导入风格
- 检测循环依赖

**使用场景**:
- 项目重构时
- 修改包结构后
- 统一代码风格

---

## 🧪 测试脚本（Tests）

### `test_risk_config.py` ✅
**位置**: `scripts/tests/`
**用途**: 手动测试风险配置系统
**使用方式**:
```bash
uv run python scripts/tests/test_risk_config.py
```

**功能**:
- 测试风险配置加载
- 验证 AgentManager 初始化
- 测试板块映射
- 验证自定义风险配置

**测试内容**:
1. 从 .env 加载风险配置
2. PortfolioRiskConfig.from_config()
3. AgentManager 初始化
4. 板块映射加载
5. 自定义风险配置

**适用场景**:
- 修改风险配置后验证
- 学习风险管理系统
- 调试配置问题

**注意**: 这是手动测试脚本，正式测试应使用 `pytest tests/`

---

## 🎓 教学脚本（Teaching）

### `student_setup.sh` 🚀 一键配置
**位置**: `scripts/teaching/`
**用途**: 学生环境一键配置脚本
**使用方式**:
```bash
bash scripts/teaching/student_setup.sh
```

**功能**:
- 自动检查 Python 版本
- 安装 uv 包管理器
- 创建虚拟环境
- 安装所有依赖
- 创建 .env 配置文件
- 启动数据库服务（Docker）

**流程**:
```
1. ✅ 检查 Python 3.12+
2. ✅ 安装 uv（如果未安装）
3. ✅ 创建虚拟环境
4. ✅ 安装依赖 (uv sync)
5. ✅ 创建 .env 文件
6. ✅ 启动 MongoDB + Redis
7. ✅ 运行环境验证
8. ✅ 运行第一个示例
```

**适用人群**:
- ✅ 初学者
- ✅ 第一次使用项目的学生
- ✅ 需要快速配置环境

---

### `verify_environment.py` ✅ 环境验证
**位置**: `scripts/teaching/`
**用途**: 验证开发环境是否正确配置
**使用方式**:
```bash
uv run python scripts/teaching/verify_environment.py
```

**功能**:
- 检查 Python 版本
- 检查 uv 包管理器
- 检查 .env 配置文件
- 检查 Python 依赖
- 检查 MongoDB 连接
- 检查 Redis 连接
- 检查项目目录结构

**输出示例**:
```
============================================================
🔍 CherryQuant 环境验证
============================================================

📋 基础环境检查:
  ✅ Python 版本: Python 3.12.0
  ✅ uv 包管理器: uv 0.1.9
  ✅ .env 配置文件: /path/to/.env
  ✅ Python 依赖: 所有关键依赖已安装
  ✅ 项目目录结构: 所有必需目录存在

🗄️  数据库服务检查:
  ✅ MongoDB: localhost:27017
  ✅ Redis: localhost:6379

============================================================
📊 检查结果: 7/7 项通过 (100.0%)
============================================================
```

**适用场景**:
- 完成环境配置后验证
- Lab 01 实验要求
- 故障排查

---

## 🗄️ SQL 脚本（SQL）

### `create_agent_tables.sql` 📋
**位置**: `scripts/sql/`
**用途**: 创建多代理系统的数据表（PostgreSQL）
**使用方式**:
```bash
psql -d cherryquant -f scripts/sql/create_agent_tables.sql
```

**表结构**:
- `agents` - Agent 配置表
- `agent_decisions` - Agent 决策记录

**注意**: 当前项目使用 MongoDB，此脚本为历史遗留，仅供参考

---

### `create_portfolio_tables.sql` 📊
**位置**: `scripts/sql/`
**用途**: 创建组合管理的数据表（PostgreSQL）
**使用方式**:
```bash
psql -d cherryquant -f scripts/sql/create_portfolio_tables.sql
```

**表结构**:
- `portfolios` - 组合配置
- `portfolio_positions` - 组合持仓

**注意**: 当前项目使用 MongoDB，此脚本为历史遗留，仅供参考

---

## 🎯 使用建议

### 按用户角色推荐

**初学者（学生）**:
1. ✅ `scripts/teaching/student_setup.sh` - 一键配置环境
2. ✅ `scripts/teaching/verify_environment.py` - 验证环境
3. ✅ `start.sh` - 交互式启动（根目录）
4. ✅ `run_cherryquant.py` - 学习基本用法（根目录）

**开发者**:
1. ✅ `run_cherryquant.py` - 日常开发和测试（根目录）
2. ✅ `scripts/data/init_historical_data.py` - 初始化数据
3. ✅ `scripts/utils/fix_env_config.py` - 配置管理
4. ✅ `scripts/tests/test_risk_config.py` - 手动测试

**研究者**:
1. ✅ `scripts/runners/run_cherryquant_ai_selection.py` - AI选股研究
2. ✅ `scripts/runners/run_cherryquant_multi_agent.py` - 多Agent研究
3. ✅ `scripts/runners/run_cherryquant_complete.py` - 完整系统

**运维人员**:
1. ✅ `scripts/utils/migrate_postgres_to_mongodb.py` - 数据迁移
2. ✅ `scripts/sql/*.sql` - 数据库管理
3. ✅ `scripts/data/init_historical_data.py` - 数据初始化

---

## 📝 脚本开发规范

### 新增脚本时

1. **选择正确的目录**:
   - 运行脚本 → `runners/`
   - 数据处理 → `data/`
   - 工具脚本 → `utils/`
   - 测试脚本 → `tests/`
   - 教学脚本 → `teaching/`
   - SQL脚本 → `sql/`

2. **脚本模板**:
```python
#!/usr/bin/env python3
"""
脚本名称: 简短描述

详细说明：
- 功能1
- 功能2

使用方式:
    uv run python scripts/category/script_name.py [args]

参数:
    --arg1: 参数说明

示例:
    uv run python scripts/category/script_name.py --arg1 value
"""

import argparse
import sys
from pathlib import Path

# 添加项目根目录到路径（如果需要）
project_root = Path(__file__).parent.parent.parent
sys.path.insert(0, str(project_root))

def main():
    """主函数"""
    parser = argparse.ArgumentParser(description="脚本描述")
    parser.add_argument("--arg1", help="参数说明")
    args = parser.parse_args()

    # 实现逻辑
    print("✅ 脚本执行成功")

if __name__ == "__main__":
    main()
```

3. **文档要求**:
   - 添加到本 README
   - 包含用途、使用方式、适用场景
   - 提供使用示例

---

## 🔍 常见问题

### Q: 为什么有多个运行脚本？

A: 不同脚本针对不同使用场景：
- `run_cherryquant.py`: 通用主脚本，日常使用
- `start.sh`: 新手友好，交互式
- `runners/` 下的脚本: 特定功能和高级特性

### Q: 脚本在哪里运行？

A: 所有脚本都从**项目根目录**运行：
```bash
cd /path/to/CherryQuant
uv run python scripts/category/script_name.py
```

### Q: 如何选择合适的脚本？

A: 参考本文档的"使用建议"部分，按用户角色选择。

---

**最后更新**: 2024-11-19
**维护者**: CherryQuant Team
